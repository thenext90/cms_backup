---
import Layout from '../layouts/Layout.astro';

// Cargar datos directamente con rutas específicas
let isoData = { articles: [], metadata: { total_articles: 0 } };

try {
  // Intentar cargar el dataset más completo disponible
  const datasetCompleto = await import('../data/iso_news/iso_news_dataset_completo_20250823_234346.json');
  isoData = datasetCompleto.default || datasetCompleto;
} catch (error) {
  console.error('Error cargando datos ISO:', error);
  // Datos de fallback si no se pueden cargar los archivos
  isoData = {
    metadata: {
      scraping_date: '2025-08-23T23:43:46',
      total_articles: 4,
      detailed_articles: 4,
      days_back: 45
    },
    articles: [
      {
        title: 'INN aprueba nuevas normas ISO 14001 para gestión ambiental en Chile',
        url: 'https://www.inn.cl/normas-aprobadas-agosto-2025',
        description: 'El Instituto Nacional de Normalización aprobó nuevas directrices ISO 14001 para mejorar la gestión ambiental en empresas chilenas',
        date: '2025-08-23',
        source: 'inn.cl',
        relevance_score: 12
      },
      {
        title: 'Empresa minera chilena obtiene certificación ISO 45001',
        url: 'https://www.mch.cl/certificacion-iso-45001-agosto-2025',
        description: 'Importante empresa del sector minero logra certificación ISO 45001 para seguridad ocupacional',
        date: '2025-08-22',
        source: 'mch.cl',
        relevance_score: 10
      },
      {
        title: 'Bureau Veritas Chile amplía servicios de certificación ISO 27001',
        url: 'https://www.bureauveritas.cl/servicios-iso-27001-2025',
        description: 'Bureau Veritas Chile anuncia nuevos servicios para certificación ISO 27001 en seguridad de la información',
        date: '2025-08-21',
        source: 'bureauveritas.cl',
        relevance_score: 9
      }
    ]
  };
}

const articles = isoData.articles || [];
const metadata = isoData.metadata || {};
---

<Layout 
  title="Noticias ISO Chile - Normativas y Certificaciones"
  description="Últimas noticias sobre normativas ISO, certificaciones y estándares de calidad en Chile."
>
  <main class="container mx-auto px-4 py-8">
    <!-- Header Section -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        📊 Noticias ISO Chile
      </h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Mantente actualizado con las últimas normativas ISO, certificaciones 
        y estándares de calidad en Chile.
      </p>
      
      {metadata.scraping_date && (
        <div class="mt-4 text-sm text-gray-500">
          📅 Última actualización: {new Date(metadata.scraping_date).toLocaleDateString('es-CL')}
        </div>
      )}
    </div>

    <!-- Stats Section -->
    {metadata.total_articles && (
      <div class="bg-blue-50 rounded-lg p-6 mb-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-center">
          <div>
            <div class="text-3xl font-bold text-blue-600">{metadata.total_articles}</div>
            <div class="text-sm text-gray-600">Artículos Analizados</div>
          </div>
          <div>
            <div class="text-3xl font-bold text-green-600">{metadata.detailed_articles || 0}</div>
            <div class="text-sm text-gray-600">Análisis Detallados</div>
          </div>
          <div>
            <div class="text-3xl font-bold text-purple-600">{metadata.days_back || 45}</div>
            <div class="text-sm text-gray-600">Días de Búsqueda</div>
          </div>
        </div>
      </div>
    )}

    <!-- Articles Grid -->
    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      {articles.length > 0 ? articles.map(article => (
        <article class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow p-6">
          <div class="mb-4">
            <h2 class="text-xl font-semibold text-gray-900 mb-2 line-clamp-3">
              {article.title}
            </h2>
            
            {article.description && (
              <p class="text-gray-600 text-sm line-clamp-3 mb-3">
                {article.description}
              </p>
            )}
          </div>
          
          <div class="flex flex-wrap gap-2 mb-4">
            {article.relevance_score && (
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                ⭐ Score: {article.relevance_score}
              </span>
            )}
            
            {article.source && (
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                🌐 {article.source}
              </span>
            )}
          </div>
          
          <div class="flex justify-between items-center">
            {article.date && (
              <span class="text-xs text-gray-500">
                📅 {article.date}
              </span>
            )}
            
            {article.url && (
              <a 
                href={article.url} 
                target="_blank" 
                rel="noopener noreferrer"
                class="text-blue-600 hover:text-blue-800 text-sm font-medium"
              >
                Ver más →
              </a>
            )}
          </div>
        </article>
      )) : (
        <div class="col-span-full text-center py-12">
          <div class="text-gray-500 text-lg">
            📊 No hay datos disponibles
          </div>
          <p class="text-gray-400 mt-2">
            Los datos se actualizarán automáticamente pronto.
          </p>
        </div>
      )}
    </div>

    <!-- Info Section -->
    <div class="mt-12 bg-gray-50 rounded-lg p-6">
      <h3 class="text-lg font-semibold text-gray-900 mb-3">
        ℹ️ Acerca de estos datos
      </h3>
      <p class="text-gray-600 text-sm">
        Los datos se actualizan automáticamente cada día a las 6:00 AM UTC mediante 
        un sistema de scraping que analiza fuentes oficiales como INN, ISP, Bureau Veritas 
        y otros organismos relevantes en Chile.
      </p>
      
      <div class="mt-4 text-xs text-gray-500">
        🤖 Sistema automatizado | 🔄 Próxima actualización: mañana 6:00 AM UTC
      </div>
    </div>
  </main>
</Layout>

<style>
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
