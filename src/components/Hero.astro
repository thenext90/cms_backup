---
import { ArrowRight, CheckCircle, Star } from 'lucide-astro';
---

<section class="relative text-white py-6 sm:py-8 lg:py-12 overflow-hidden hero-section">
  <video
    class="absolute top-0 left-0 w-full h-full object-cover -z-10 hero-video"
    src="https://media.publit.io/file/Generated-File-August-27-2025-12-26AM.mp4"
    autoplay
    loop
    muted
    playsinline
    poster="/images/capa-cms-banner.webp"
  >
  </video>
  <div class="absolute top-0 left-0 w-full h-full bg-black/40 z-0 hero-overlay"></div>

  <!-- Elementos decorativos de fondo -->
  <div class="absolute inset-0">
    <div class="absolute top-10 left-10 w-32 h-32 bg-white/5 rounded-full blur-xl"></div>
    <div class="absolute bottom-10 right-10 w-48 h-48 bg-orange-400/10 rounded-full blur-2xl"></div>
    <div class="absolute top-1/2 left-1/3 w-24 h-24 bg-yellow-400/5 rounded-full blur-lg"></div>
  </div>
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full relative z-10">
    <!-- Layout de una sola columna, centrado -->
    <div class="flex flex-col items-center justify-center min-h-[50vh] text-center">
      <div class="max-w-2xl space-y-2 sm:space-y-3 relative z-10 w-full">
        <div class="space-y-2">
          <div class="inline-flex items-center space-x-2 bg-gradient-to-r from-orange-400/20 to-yellow-400/20 backdrop-blur-sm border border-orange-300/30 rounded-full px-3 sm:px-4 py-2">
            <Star size="16" class="sm:w-5 sm:h-5 fill-current text-yellow-300 flex-shrink-0" />
            <span class="text-xs sm:text-sm font-bold tracking-wide text-white">Excelencia desde 1995 • 25+ años</span>
          </div>
          
          <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold leading-tight text-shadow">
            CMS Consultores
            <span class="block text-orange-200 bg-gradient-to-r from-orange-200 to-yellow-200 bg-clip-text text-transparent">Camino a la Calidad</span>
          </h1>
          
          <p class="text-base sm:text-lg lg:text-xl text-orange-100 leading-relaxed max-w-md mx-auto text-shadow">
            Sistemas de Gestión de Calidad y Capacitación en Control y Gestion de Procesos. 
            25+ años transformando organizaciones hacia la excelencia.
          </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-2 lg:gap-3 justify-center pt-1">
          <a href="/capacitacion" class="flex items-center justify-center space-x-2 bg-white text-accent-800 px-4 sm:px-6 lg:px-6 py-2.5 lg:py-3 rounded-xl hover:bg-red-50 transition-all duration-300 font-semibold shadow-xl hover:shadow-2xl hover:scale-105 text-sm">
            <span>Conocer Servicios</span>
            <ArrowRight size="16" />
          </a>
          
          <a href="/contacto" class="flex items-center justify-center space-x-2 border-2 border-white text-white px-4 sm:px-6 lg:px-6 py-2.5 lg:py-3 rounded-xl hover:bg-white hover:text-accent-800 transition-all duration-300 font-semibold backdrop-blur-sm text-sm">
            <span>Contactar Ahora</span>
          </a>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-2 pt-2 border-t border-white/20">
          <div class="flex items-center justify-center space-x-2">
            <CheckCircle size="16" class="sm:w-[18px] sm:h-[18px] text-green-300 flex-shrink-0" />
            <span class="text-xs sm:text-xs font-medium">Certificaciones ISO</span>
          </div>
          <div class="flex items-center justify-center space-x-2">
            <CheckCircle size="16" class="sm:w-[18px] sm:h-[18px] text-green-300 flex-shrink-0" />
            <span class="text-xs sm:text-xs font-medium">Capacitación</span>
          </div>
          <div class="flex items-center justify-center space-x-2">
            <CheckCircle size="16" class="sm:w-[18px] sm:h-[18px] text-green-300 flex-shrink-0" />
            <span class="text-xs sm:text-xs font-medium">Consultoría Integral</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
.slider-container {
  position: relative;
  width: 100%;
  max-width: 100%;
}

.slider {
  position: relative;
  width: 100%;
  height: auto;
  overflow: hidden;
  border-radius: 16px;
}

.slide {
  display: none;
  position: relative;
  width: 100%;
  height: 100%;
}

.slide.active {
  display: block;
}

.slide img {
  width: 100%;
  height: auto;
  object-fit: cover;
}

.nav-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background-color: rgba(0, 0, 0, 0.6);
  color: white;
  border: none;
  padding: 14px 18px;
  cursor: pointer;
  font-size: 18px;
  border-radius: 6px;
  user-select: none;
  z-index: 20;
  transition: all 0.3s ease;
  backdrop-filter: blur(4px);
}

.nav-btn:hover {
  background-color: rgba(0, 0, 0, 0.8);
  transform: translateY(-50%) scale(1.1);
}

.prev-btn {
  left: 15px;
}

.next-btn {
  right: 15px;
}

.dots-container {
  text-align: center;
  margin-top: 12px;
  position: relative;
  z-index: 20;
}

.dot {
  cursor: pointer;
  height: 12px;
  width: 12px;
  margin: 0 5px;
  background-color: rgba(255, 255, 255, 0.5);
  border-radius: 50%;
  display: inline-block;
  transition: all 0.3s ease;
  border: 1px solid transparent;
}

.dot.active,
.dot:hover {
  background-color: rgba(255, 255, 255, 0.95);
  transform: scale(1.1);
  border-color: rgba(255, 255, 255, 0.7);
}

/* Fade animation */
.slide {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.slide.active {
  opacity: 1;
}

/* Slide overlay and text */
.slide {
  position: relative;
}

.slide-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    45deg,
    rgba(139, 69, 19, 0.8) 0%,
    rgba(160, 82, 45, 0.6) 50%,
    rgba(0, 0, 0, 0.4) 100%
  );
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 1rem;
  z-index: 10;
}

.slide-content {
  text-align: center;
  padding: 2.5rem 2rem;
  max-width: 85%;
}

.slide-title {
  font-size: 2rem;
  font-weight: 700;
  color: white;
  margin-bottom: 1rem;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  line-height: 1.2;
}

.slide-description {
  font-size: 1.1rem;
  color: rgba(255, 255, 255, 0.95);
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
  line-height: 1.5;
  font-weight: 400;
  max-width: 500px;
  margin: 0 auto;
}

/* Responsive text sizing - Layout más simétrico */
@media (min-width: 1024px) {
  .slider-container {
    max-width: none;
    width: calc(100% + 240px); /* Aumentar 120px por cada lado = 240px total */
    min-width: 670px; /* 550px + 120px */
    margin-left: -120px; /* Centrar el contenido expandido */
  }
  
  .slide img {
    height: 340px; /* 420px - 80px = 340px */
    object-fit: cover;
    width: 100%;
  }
  
  .slide-title {
    font-size: 2rem;
  }
  
  .slide-description {
    font-size: 1.1rem;
  }
  
  .slide-content {
    padding: 2rem 1.5rem;
    max-width: 80%;
  }
}

/* Optimización para pantallas extra grandes */
@media (min-width: 1280px) {
  .slider-container {
    min-width: 720px; /* 600px + 120px */
    width: calc(100% + 240px); /* Mantener el aumento de 120px por lado */
    margin-left: -120px;
  }
  
  .slide img {
    height: 360px; /* 440px - 80px = 360px */
  }
  
  .slide-title {
    font-size: 2.3rem;
  }
  
  .slide-description {
    font-size: 1.2rem;
    max-width: 520px;
  }
  
  .slide-content {
    padding: 2.5rem 2rem;
  }
}

/* Mejoras para tablet */
@media (max-width: 1023px) and (min-width: 769px) {
  .slider-container {
    max-width: 570px; /* 450px + 120px */
    width: calc(100% + 120px); /* Aumento proporcional para tablets */
    margin-left: -60px; /* Centrar el contenido expandido */
  }
  
  .slide img {
    height: 280px; /* 360px - 80px = 280px */
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.5rem;
  }
  
  .slide-description {
    font-size: 1rem;
  }
  
  .slide-content {
    padding: 2rem;
  }
}

/* Mejoras para móvil */
@media (max-width: 768px) {
  .slider-container {
    max-width: 100%;
  }
  
  .slide img {
    height: 190px; /* 250px - 60px (proportional reduction) */
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.3rem;
  }
  
  .slide-description {
    font-size: 0.9rem;
  }
  
  .slide-content {
    padding: 1.5rem;
  }
  
  /* Botones de navegación más pequeños en móvil */
  .nav-btn {
    padding: 8px 12px;
    font-size: 14px;
  }
  
  /* Dots más pequeños en móvil */
  .dot {
    height: 10px;
    width: 10px;
    margin: 0 3px;
  }
}

/* Mejoras para móviles pequeños */
@media (max-width: 480px) {
  .slide img {
    height: 150px; /* 200px - 50px (proportional reduction) */
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.1rem;
  }
  
  .slide-description {
    font-size: 0.8rem;
  }
  
  .slide-content {
    padding: 1rem;
  }
  
  .nav-btn {
    padding: 6px 10px;
    font-size: 12px;
  }
  
  .dot {
    height: 8px;
    width: 8px;
    margin: 0 2px;
  }
}
.text-shadow {
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.7);
}
@media (max-width: 768px) {
  .hero-video, .hero-overlay {
    display: none;
  }
  .hero-section {
    background-image: url('/images/capa-cms-banner.webp');
    background-size: cover;
    background-position: center;
  }
}
</style>