---
import { ArrowRight, CheckCircle, Star } from 'lucide-astro';
---

<section class="relative bg-gradient-to-br from-accent-900 via-accent-800 to-accent-700 text-white py-4 sm:py-6 lg:py-12 xl:py-16 overflow-hidden min-h-screen flex items-center">
  <!-- Elementos decorativos de fondo -->
  <div class="absolute inset-0">
    <div class="absolute top-10 left-10 w-32 h-32 bg-white/5 rounded-full blur-xl"></div>
    <div class="absolute bottom-10 right-10 w-48 h-48 bg-orange-400/10 rounded-full blur-2xl"></div>
    <div class="absolute top-1/2 left-1/3 w-24 h-24 bg-yellow-400/5 rounded-full blur-lg"></div>
  </div>
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-12 xl:px-16 w-full">
    <!-- Layout balanceado: contenido compacto, slider prominente -->
    <div class="flex flex-col lg:flex-row gap-4 lg:gap-16 xl:gap-20 items-center lg:items-center min-h-[75vh] lg:min-h-[70vh]">
      <div class="lg:w-[400px] xl:w-[450px] 2xl:w-[500px] lg:flex-shrink-0 space-y-2 sm:space-y-3 lg:space-y-5 relative z-10 w-full lg:max-w-none">
        <div class="space-y-3 lg:space-y-4 text-center lg:text-left">
          <div class="inline-flex items-center space-x-2 bg-gradient-to-r from-orange-400/20 to-yellow-400/20 backdrop-blur-sm border border-orange-300/30 rounded-full px-3 sm:px-4 py-2">
            <Star size="16" class="sm:w-5 sm:h-5 fill-current text-yellow-300 flex-shrink-0" />
            <span class="text-xs sm:text-sm font-bold tracking-wide text-white text-center">Excelencia desde 1995 • 25+ años</span>
          </div>
          
          <h1 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl 2xl:text-7xl font-bold leading-tight text-center lg:text-left">
            CMS Consultores
            <span class="block text-orange-200 bg-gradient-to-r from-orange-200 to-yellow-200 bg-clip-text text-transparent">Tu Socio Estratégico</span>
            <span class="block text-base sm:text-lg md:text-xl lg:text-2xl xl:text-3xl 2xl:text-4xl font-normal text-orange-100">
              en Calidad y Gestión
            </span>
          </h1>
          
          <p class="text-sm sm:text-base lg:text-lg xl:text-xl 2xl:text-2xl text-orange-100 leading-relaxed max-w-md lg:max-w-lg xl:max-w-xl text-center lg:text-left">
            Especialistas en sistemas de gestión de calidad y certificaciones ISO. 
            Más de 25 años transformando organizaciones hacia la excelencia operacional y mejora continua.
          </p>
        </div>

                <div class="flex flex-col sm:flex-row gap-3 lg:gap-4 xl:gap-5 justify-center lg:justify-start pt-2 lg:pt-4">
          <a href="/servicios" class="flex items-center justify-center space-x-2 bg-white text-accent-800 px-5 sm:px-6 lg:px-8 xl:px-10 py-3 lg:py-4 xl:py-5 rounded-xl hover:bg-red-50 transition-all duration-300 font-semibold shadow-xl hover:shadow-2xl hover:scale-105 text-sm lg:text-base xl:text-lg">
            <span>Conocer Servicios</span>
            <ArrowRight size="16" />
          </a>
          
          <a href="/contacto" class="flex items-center justify-center space-x-2 border-2 border-white text-white px-5 sm:px-6 lg:px-8 xl:px-10 py-3 lg:py-4 xl:py-5 rounded-xl hover:bg-white hover:text-accent-800 transition-all duration-300 font-semibold backdrop-blur-sm text-sm lg:text-base xl:text-lg">
            <span>Contactar Ahora</span>
          </a>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 lg:gap-4 pt-4 lg:pt-6 border-t border-white/20">
          <div class="flex items-center justify-center lg:justify-start space-x-2">
            <CheckCircle size="16" class="sm:w-[18px] sm:h-[18px] text-green-300 flex-shrink-0" />
            <span class="text-xs sm:text-xs font-medium">Certificaciones ISO</span>
          </div>
          <div class="flex items-center justify-center lg:justify-start space-x-2">
            <CheckCircle size="16" class="sm:w-[18px] sm:h-[18px] text-green-300 flex-shrink-0" />
            <span class="text-xs sm:text-xs font-medium">Capacitación</span>
          </div>
          <div class="flex items-center justify-center lg:justify-start space-x-2">
            <CheckCircle size="16" class="sm:w-[18px] sm:h-[18px] text-green-300 flex-shrink-0" />
            <span class="text-xs sm:text-xs font-medium">Consultoría Integral</span>
          </div>
        </div>
      </div>

      <!-- Slider optimizado para mejor distribución -->
      <div class="w-full max-w-sm sm:max-w-md lg:flex-1 lg:min-w-0 lg:max-w-none xl:min-w-[600px] 2xl:min-w-[700px] relative z-10">
        <div class="relative z-10">
          <!-- Slider Container -->
          <div class="slider-container">
            <div class="slider" id="hero-slider">
              <div class="slide active">
                <img src="https://images.unsplash.com/photo-1600880292203-757bb62b4baf?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Equipo de Consultores Profesionales" class="rounded-2xl shadow-2xl w-full h-auto" />
                <div class="slide-overlay">
                  <div class="slide-content">
                    <h2 class="slide-title">Equipo de Consultores Expertos</h2>
                    <p class="slide-description">Más de 25 años transformando organizaciones a través de la excelencia operacional</p>
                  </div>
                </div>
              </div>
              <div class="slide">
                <img src="https://images.unsplash.com/photo-1521737604893-d14cc237f11d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Reunión de Negocios Corporativa" class="rounded-2xl shadow-2xl w-full h-auto" />
                <div class="slide-overlay">
                  <div class="slide-content">
                    <h2 class="slide-title">Consultoría Estratégica</h2>
                    <p class="slide-description">Implementamos sistemas de gestión de calidad que impulsan el crecimiento sostenible</p>
                  </div>
                </div>
              </div>
              <div class="slide">
                <img src="https://images.unsplash.com/photo-1556745757-8d76bdb6984b?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Consultores Analizando Datos" class="rounded-2xl shadow-2xl w-full h-auto" />
                <div class="slide-overlay">
                  <div class="slide-content">
                    <h2 class="slide-title">Certificaciones ISO</h2>
                    <p class="slide-description">Acompañamos a tu empresa hacia la certificación ISO y la mejora continua</p>
                  </div>
                </div>
              </div>
            </div>
            
            <!-- Navigation buttons -->
            <button class="nav-btn prev-btn" id="hero-prev-btn">&#10094;</button>
            <button class="nav-btn next-btn" id="hero-next-btn">&#10095;</button>
            
            <!-- Dots indicator -->
            <div class="dots-container">
              <span class="dot active" data-slide="1"></span>
              <span class="dot" data-slide="2"></span>
              <span class="dot" data-slide="3"></span>
            </div>
          </div>
        </div>
        
        <!-- Elementos decorativos optimizados -->
        <div class="absolute -top-8 -right-8 w-36 h-36 bg-gradient-to-br from-orange-400 to-yellow-400 rounded-full opacity-20 blur-xl lg:-top-10 lg:-right-10 lg:w-48 lg:h-48"></div>
        <div class="absolute -bottom-12 -left-12 w-44 h-44 bg-gradient-to-br from-orange-300 to-red-300 rounded-full opacity-15 blur-2xl lg:-bottom-16 lg:-left-16 lg:w-56 lg:h-56"></div>
      </div>
    </div>
  </div>
</section>

<script>
(function() {
  'use strict';
  
  let heroSlideIndex = 1;
  let autoSlideInterval: number;

  function changeHeroSlide(n: number) {
    showHeroSlide(heroSlideIndex += n);
  }

  function currentHeroSlide(n: number) {
    showHeroSlide(heroSlideIndex = n);
  }

  function showHeroSlide(n: number) {
    const slides = document.querySelectorAll('#hero-slider .slide') as NodeListOf<HTMLElement>;
    const dots = document.querySelectorAll('.dots-container .dot') as NodeListOf<HTMLElement>;
    
    if (n > slides.length) { heroSlideIndex = 1; }
    if (n < 1) { heroSlideIndex = slides.length; }
    
    // Remove active class from all slides and dots
    slides.forEach(slide => slide.classList.remove('active'));
    dots.forEach(dot => dot.classList.remove('active'));
    
    // Add active class to current slide and dot
    if (slides[heroSlideIndex - 1]) {
      slides[heroSlideIndex - 1].classList.add('active');
    }
    if (dots[heroSlideIndex - 1]) {
      dots[heroSlideIndex - 1].classList.add('active');
    }
    
    console.log('Showing slide:', heroSlideIndex);
  }

  function initSlider() {
    // Navigation buttons
    const prevBtn = document.getElementById('hero-prev-btn');
    const nextBtn = document.getElementById('hero-next-btn');
    
    if (prevBtn) {
      prevBtn.addEventListener('click', (e) => {
        e.preventDefault();
        changeHeroSlide(-1);
        console.log('Previous button clicked');
      });
    }
    
    if (nextBtn) {
      nextBtn.addEventListener('click', (e) => {
        e.preventDefault();
        changeHeroSlide(1);
        console.log('Next button clicked');
      });
    }
    
    // Dot indicators
    const dots = document.querySelectorAll('.dots-container .dot');
    dots.forEach((dot, index) => {
      dot.addEventListener('click', (e) => {
        e.preventDefault();
        const slideNumber = parseInt((dot as HTMLElement).getAttribute('data-slide') || '1');
        currentHeroSlide(slideNumber);
        console.log('Dot clicked:', slideNumber);
      });
    });
    
    // Initialize first slide
    showHeroSlide(heroSlideIndex);
    
    // Auto-slide every 5 seconds
    autoSlideInterval = setInterval(() => {
      changeHeroSlide(1);
    }, 5000) as unknown as number;
    
    console.log('Hero slider initialized');
  }

  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initSlider);
  } else {
    initSlider();
  }

  // Clean up interval when page unloads
  window.addEventListener('beforeunload', () => {
    if (autoSlideInterval) {
      clearInterval(autoSlideInterval);
    }
  });
})();
</script>

<style>
.slider-container {
  position: relative;
  width: 100%;
  max-width: 100%;
}

.slider {
  position: relative;
  width: 100%;
  height: auto;
  overflow: hidden;
  border-radius: 16px;
}

.slide {
  display: none;
  position: relative;
  width: 100%;
  height: 100%;
}

.slide.active {
  display: block;
}

.slide img {
  width: 100%;
  height: auto;
  object-fit: cover;
}

.nav-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background-color: rgba(0, 0, 0, 0.6);
  color: white;
  border: none;
  padding: 14px 18px;
  cursor: pointer;
  font-size: 18px;
  border-radius: 6px;
  user-select: none;
  z-index: 20;
  transition: all 0.3s ease;
  backdrop-filter: blur(4px);
}

.nav-btn:hover {
  background-color: rgba(0, 0, 0, 0.8);
  transform: translateY(-50%) scale(1.1);
}

.prev-btn {
  left: 15px;
}

.next-btn {
  right: 15px;
}

.dots-container {
  text-align: center;
  margin-top: 12px;
  position: relative;
  z-index: 20;
}

.dot {
  cursor: pointer;
  height: 12px;
  width: 12px;
  margin: 0 5px;
  background-color: rgba(255, 255, 255, 0.5);
  border-radius: 50%;
  display: inline-block;
  transition: all 0.3s ease;
  border: 1px solid transparent;
}

.dot.active,
.dot:hover {
  background-color: rgba(255, 255, 255, 0.95);
  transform: scale(1.1);
  border-color: rgba(255, 255, 255, 0.7);
}

/* Fade animation */
.slide {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.slide.active {
  opacity: 1;
}

/* Slide overlay and text */
.slide {
  position: relative;
}

.slide-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    45deg,
    rgba(139, 69, 19, 0.8) 0%,
    rgba(160, 82, 45, 0.6) 50%,
    rgba(0, 0, 0, 0.4) 100%
  );
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 1rem;
  z-index: 10;
}

.slide-content {
  text-align: center;
  padding: 2.5rem 2rem;
  max-width: 85%;
}

.slide-title {
  font-size: 2rem;
  font-weight: 700;
  color: white;
  margin-bottom: 1rem;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  line-height: 1.2;
}

.slide-description {
  font-size: 1.1rem;
  color: rgba(255, 255, 255, 0.95);
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
  line-height: 1.5;
  font-weight: 400;
  max-width: 500px;
  margin: 0 auto;
}

/* Responsive text sizing - Layout más simétrico */
@media (min-width: 1024px) {
  .slider-container {
    max-width: none;
    width: 100%;
    min-width: 600px;
  }
  
  .slide img {
    height: 400px; /* Altura optimizada para desktop */
    object-fit: cover;
    width: 100%;
  }
  
  .slide-title {
    font-size: 2.2rem;
  }
  
  .slide-description {
    font-size: 1.2rem;
  }
  
  .slide-content {
    padding: 3rem 2.5rem;
    max-width: 85%;
  }
}

/* Optimización para pantallas extra grandes */
@media (min-width: 1280px) {
  .slider-container {
    min-width: 700px;
  }
  
  .slide img {
    height: 450px; /* Altura mayor para XL */
  }
  
  .slide-title {
    font-size: 2.5rem;
  }
  
  .slide-description {
    font-size: 1.3rem;
    max-width: 550px;
  }
  
  .slide-content {
    padding: 3.5rem 3rem;
  }
}

/* Optimización para pantallas 2XL */
@media (min-width: 1536px) {
  .slider-container {
    min-width: 800px;
  }
  
  .slide img {
    height: 500px; /* Altura premium para 2XL */
  }
  
  .slide-title {
    font-size: 3rem;
  }
  
  .slide-description {
    font-size: 1.5rem;
    max-width: 600px;
  }
  
  .slide-content {
    padding: 4rem 3.5rem;
  }
}

/* Mejoras para tablet */
@media (max-width: 1023px) and (min-width: 769px) {
  .slider-container {
    max-width: 450px;
  }
  
  .slide img {
    height: 300px;
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.5rem;
  }
  
  .slide-description {
    font-size: 1rem;
  }
  
  .slide-content {
    padding: 2rem;
  }
}

/* Mejoras para móvil */
@media (max-width: 768px) {
  .slider-container {
    max-width: 100%;
  }
  
  .slide img {
    height: 250px;
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.3rem;
  }
  
  .slide-description {
    font-size: 0.9rem;
  }
  
  .slide-content {
    padding: 1.5rem;
  }
  
  /* Botones de navegación más pequeños en móvil */
  .nav-btn {
    padding: 8px 12px;
    font-size: 14px;
  }
  
  /* Dots más pequeños en móvil */
  .dot {
    height: 10px;
    width: 10px;
    margin: 0 3px;
  }
}

/* Mejoras para móviles pequeños */
@media (max-width: 480px) {
  .slide img {
    height: 200px;
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.1rem;
  }
  
  .slide-description {
    font-size: 0.8rem;
  }
  
  .slide-content {
    padding: 1rem;
  }
  
  .nav-btn {
    padding: 6px 10px;
    font-size: 12px;
  }
  
  .dot {
    height: 8px;
    width: 8px;
    margin: 0 2px;
  }
}
</style>