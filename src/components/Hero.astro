---
import { ArrowRight, CheckCircle, Star } from 'lucide-astro';
---

<section class="relative bg-gradient-to-br from-accent-900 via-accent-800 to-accent-700 text-white py-12 overflow-hidden">
  <!-- Elementos decorativos de fondo -->
  <div class="absolute inset-0">
    <div class="absolute top-10 left-10 w-32 h-32 bg-white/5 rounded-full blur-xl"></div>
    <div class="absolute bottom-10 right-10 w-48 h-48 bg-orange-400/10 rounded-full blur-2xl"></div>
    <div class="absolute top-1/2 left-1/3 w-24 h-24 bg-yellow-400/5 rounded-full blur-lg"></div>
  </div>
  
  <div class="max-w-container mx-auto px-4">
    <!-- Layout balanceado: contenido compacto a la izquierda, slider prominente a la derecha -->
    <div class="flex flex-col lg:flex-row gap-8 items-center">
      <div class="lg:flex-1 lg:max-w-lg space-y-6 relative z-10">
        <div class="space-y-3">
          <div class="inline-flex items-center space-x-2 bg-gradient-to-r from-orange-400/20 to-yellow-400/20 backdrop-blur-sm border border-orange-300/30 rounded-full px-4 py-2">
            <Star size="20" class="fill-current text-yellow-300" />
            <span class="text-sm font-bold tracking-wide text-white">Excelencia desde 1995 • Más de 25 años transformando empresas</span>
          </div>
          
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold leading-tight">
            CMS Consultores
            <span class="block text-orange-200 bg-gradient-to-r from-orange-200 to-yellow-200 bg-clip-text text-transparent">Tu Socio Estratégico</span>
            <span class="block text-xl md:text-2xl lg:text-3xl font-normal text-orange-100">
              en Calidad y Gestión
            </span>
          </h1>
          
          <p class="text-base md:text-lg text-orange-100 leading-relaxed max-w-xl">
            Especialistas en sistemas de gestión de calidad, certificaciones ISO 
            y capacitación empresarial. Más de 25 años transformando organizaciones.
          </p>
        </div>

        <div class="flex flex-wrap gap-3">
          <a href="/servicios" class="flex items-center space-x-2 bg-white text-accent-800 px-6 py-3 rounded-xl hover:bg-red-50 transition-all duration-300 font-semibold shadow-xl hover:shadow-2xl hover:scale-105 text-sm">
            <span>Conocer Servicios</span>
            <ArrowRight size="16" />
          </a>
          
          <a href="/contacto" class="flex items-center space-x-2 border-2 border-white text-white px-6 py-3 rounded-xl hover:bg-white hover:text-accent-800 transition-all duration-300 font-semibold backdrop-blur-sm text-sm">
            <span>Contactar Ahora</span>
          </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 pt-6 border-t border-white/20">
          <div class="flex items-center space-x-2">
            <CheckCircle size="18" class="text-green-300 flex-shrink-0" />
            <span class="text-xs font-medium">Certificaciones ISO</span>
          </div>
          <div class="flex items-center space-x-2">
            <CheckCircle size="18" class="text-green-300 flex-shrink-0" />
            <span class="text-xs font-medium">Capacitación Especializada</span>
          </div>
          <div class="flex items-center space-x-2">
            <CheckCircle size="18" class="text-green-300 flex-shrink-0" />
            <span class="text-xs font-medium">Consultoría Integral</span>
          </div>
        </div>
      </div>

      <!-- Slider mucho más grande y prominente para desktop -->
      <div class="lg:w-[600px] lg:flex-shrink-0 w-full relative z-10">
        <div class="relative z-10">
          <!-- Slider Container -->
          <div class="slider-container">
            <div class="slider" id="hero-slider">
              <div class="slide active">
                <img src="https://images.unsplash.com/photo-1600880292203-757bb62b4baf?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Equipo de Consultores Profesionales" class="rounded-2xl shadow-2xl w-full h-auto" />
                <div class="slide-overlay">
                  <div class="slide-content">
                    <h2 class="slide-title">Equipo de Consultores Expertos</h2>
                    <p class="slide-description">Más de 25 años transformando organizaciones a través de la excelencia operacional</p>
                  </div>
                </div>
              </div>
              <div class="slide">
                <img src="https://images.unsplash.com/photo-1521737604893-d14cc237f11d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Reunión de Negocios Corporativa" class="rounded-2xl shadow-2xl w-full h-auto" />
                <div class="slide-overlay">
                  <div class="slide-content">
                    <h2 class="slide-title">Consultoría Estratégica</h2>
                    <p class="slide-description">Implementamos sistemas de gestión de calidad que impulsan el crecimiento sostenible</p>
                  </div>
                </div>
              </div>
              <div class="slide">
                <img src="https://images.unsplash.com/photo-1556745757-8d76bdb6984b?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Consultores Analizando Datos" class="rounded-2xl shadow-2xl w-full h-auto" />
                <div class="slide-overlay">
                  <div class="slide-content">
                    <h2 class="slide-title">Certificaciones ISO</h2>
                    <p class="slide-description">Acompañamos a tu empresa hacia la certificación ISO y la mejora continua</p>
                  </div>
                </div>
              </div>
            </div>
            
            <!-- Navigation buttons -->
            <button class="nav-btn prev-btn" onclick="changeHeroSlide(-1)">&#10094;</button>
            <button class="nav-btn next-btn" onclick="changeHeroSlide(1)">&#10095;</button>
            
            <!-- Dots indicator -->
            <div class="dots-container">
              <span class="dot active" onclick="currentHeroSlide(1)"></span>
              <span class="dot" onclick="currentHeroSlide(2)"></span>
              <span class="dot" onclick="currentHeroSlide(3)"></span>
            </div>
          </div>
        </div>
        
        <!-- Elementos decorativos -->
        <div class="absolute -top-6 -right-6 w-32 h-32 bg-gradient-to-br from-orange-400 to-yellow-400 rounded-full opacity-20 blur-xl"></div>
        <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-gradient-to-br from-orange-300 to-red-300 rounded-full opacity-15 blur-2xl"></div>
      </div>
    </div>
  </div>
</section>

<script>
  let heroSlideIndex = 1;

  function changeHeroSlide(n: number) {
    showHeroSlide(heroSlideIndex += n);
  }

  function currentHeroSlide(n: number) {
    showHeroSlide(heroSlideIndex = n);
  }

  function showHeroSlide(n: number) {
    const slides = document.querySelectorAll('#hero-slider .slide');
    const dots = document.querySelectorAll('#hero-slider ~ .dots-container .dot');
    
    if (n > slides.length) { heroSlideIndex = 1; }
    if (n < 1) { heroSlideIndex = slides.length; }
    
    slides.forEach(slide => slide.classList.remove('active'));
    dots.forEach(dot => dot.classList.remove('active'));
    
    if (slides[heroSlideIndex - 1]) {
      slides[heroSlideIndex - 1].classList.add('active');
    }
    if (dots[heroSlideIndex - 1]) {
      dots[heroSlideIndex - 1].classList.add('active');
    }
  }

  // Auto-slide every 5 seconds
  const autoSlideInterval = setInterval(() => {
    changeHeroSlide(1);
  }, 5000);

  // Initialize slider
  document.addEventListener('DOMContentLoaded', function() {
    showHeroSlide(heroSlideIndex);
  });

  // Clean up interval when page unloads
  window.addEventListener('beforeunload', () => {
    clearInterval(autoSlideInterval);
  });
</script>

<style>
.slider-container {
  position: relative;
  width: 100%;
  max-width: 100%;
}

.slider {
  position: relative;
  width: 100%;
  height: auto;
  overflow: hidden;
}

.slide {
  display: none;
  position: relative;
  width: 100%;
  height: 100%;
}

.slide.active {
  display: block;
}

.slide img {
  width: 100%;
  height: auto;
  object-fit: cover;
}

.nav-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background-color: rgba(0, 0, 0, 0.5);
  color: white;
  border: none;
  padding: 12px 16px;
  cursor: pointer;
  font-size: 16px;
  border-radius: 0 3px 3px 0;
  user-select: none;
  z-index: 20;
  transition: background-color 0.3s;
}

.nav-btn:hover {
  background-color: rgba(0, 0, 0, 0.8);
}

.prev-btn {
  left: 10px;
  border-radius: 0 3px 3px 0;
}

.next-btn {
  right: 10px;
  border-radius: 3px 0 0 3px;
}

.dots-container {
  text-align: center;
  margin-top: 15px;
  position: relative;
  z-index: 20;
}

.dot {
  cursor: pointer;
  height: 12px;
  width: 12px;
  margin: 0 4px;
  background-color: rgba(255, 255, 255, 0.5);
  border-radius: 50%;
  display: inline-block;
  transition: background-color 0.3s;
}

.dot.active,
.dot:hover {
  background-color: rgba(255, 255, 255, 0.9);
}

/* Fade animation */
.slide {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.slide.active {
  opacity: 1;
}

/* Slide overlay and text */
.slide {
  position: relative;
}

.slide-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    45deg,
    rgba(139, 69, 19, 0.8) 0%,
    rgba(160, 82, 45, 0.6) 50%,
    rgba(0, 0, 0, 0.4) 100%
  );
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 1rem;
  z-index: 10;
}

.slide-content {
  text-align: center;
  padding: 2rem;
  max-width: 90%;
}

.slide-title {
  font-size: 1.8rem;
  font-weight: 700;
  color: white;
  margin-bottom: 0.75rem;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  line-height: 1.2;
}

.slide-description {
  font-size: 1rem;
  color: rgba(255, 255, 255, 0.95);
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
  line-height: 1.4;
  font-weight: 400;
}

/* Responsive text sizing */
@media (min-width: 1024px) {
  .slider-container {
    max-width: 600px;
  }
  
  .slide img {
    height: 400px;
    object-fit: cover;
  }
  
  .slide-title {
    font-size: 1.8rem;
  }
  
  .slide-description {
    font-size: 1.1rem;
  }
  
  .slide-content {
    padding: 2.5rem;
  }
}

@media (max-width: 768px) {
  .slide-title {
    font-size: 1.4rem;
  }
  
  .slide-description {
    font-size: 0.9rem;
  }
  
  .slide-content {
    padding: 1.5rem;
  }
}

@media (max-width: 480px) {
  .slide-title {
    font-size: 1.2rem;
  }
  
  .slide-description {
    font-size: 0.8rem;
  }
  
  .slide-content {
    padding: 1rem;
  }
}
</style>